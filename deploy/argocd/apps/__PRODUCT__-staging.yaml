# The __PRODUCT__ product STAGING Application. It reconciles the staging overlay
# (bff head + SPA + public entry + runtime DSL bundle) into the in-cluster memql
# namespace, alongside the SEPARATE engine Application (two Applications per the
# downstream-stacks contract).
#
# INITIALLY MANUAL-SYNC (the `automated:` block is intentionally omitted): sync
# once by hand and verify the render before promoting to
# `automated: {prune: true, selfHeal: true}`.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: __PRODUCT__-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: __PRODUCT__
  source:
    repoURL: https://github.com/__PRODUCT_ORG__/__PRODUCT__.git
    targetRevision: main
    path: deploy/k8s/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: memql
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
      - ApplyOutOfSyncOnly=true
  # memql-secrets is created out-of-band (real values) and is NOT in git, so it
  # is never pruned. Replica count is HPA/autoscaler-owned; don't thrash on it.
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
