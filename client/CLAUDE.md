# __PRODUCT__-client — agent guide

The __PRODUCT__ product frontend (Vite + React + TypeScript), the `client/`
directory of the single-repo product. Its DSL is the sibling `../dsl/__PRODUCT__/`;
the shared engine is the sibling checkout `../../memql`. There is no separate
carrier repo and no product `go.work` -- the product is DSL + this client.

## Golden rule: bare ids

Canonicalization is **server-side only**. This client uses **bare short-slug ids
everywhere** — it never composes, parses, or compares canonical `v1:` ids.

- The ONLY place canonical ids live is `src/generated/concepts.ts` (the sdk-gen
  A3 shape; the DSL source of truth). Refer to concepts THROUGH its exports
  (`Concepts` / `CDCTopics` / `CDCFilters` / `topicFor` / `filterFor`), never a
  hand-written literal.
- `eslint.config.js` enforces this: `v1:` literals and `.split(':')` /
  `.lastIndexOf(':')` id surgery are lint errors. Run `npm run lint`.
- When concepts mix, key rows by `(concept, id)` via `nodeKey`, never by id alone.

## Layout

- `src/lib/memql/client.ts` — the memQL client (thin local WS client in the
  starter; swap for `@__PRODUCT_ORG__/__PRODUCT__-sdk`'s `Connection`/`QueryClient`
  once published — see README "SDK resolution").
- `src/lib/auth/identity.ts` — magic-link login against `https://identity.__DOMAIN__`.
- `src/context/Session.tsx` — session + client provider (`useSession` / `useMemql`).
- `src/generated/concepts.ts` — generated bare-ids concept/topic constants.
- `src/pages/` — `Login` (magic-link), `Home` (one query round-trip), `Live`
  (one CDC subscription via generated filter constants).

## Common tasks

| Task | Command |
|------|---------|
| Install | `npm install` |
| Typecheck | `npm run typecheck` (or `make typecheck`) |
| Build | `npm run build` (or `make build`) |
| Lint (bare-ids contract) | `npm run lint` (or `make lint`) |
| HMR inner loop | `make dev` (Vite on :8080; cluster must be up) |
| Build + import + roll the SPA image | `make image` |
| Full local stack | `make up` (delegates to the root Makefile) |

## SDK

The production SDK is `@__PRODUCT_ORG__/__PRODUCT__-sdk` (GitHub Packages),
generated by the engine's `sdk-gen` over `core DSL ∪ ../dsl/__PRODUCT__/`. The
starter shell is self-contained (no SDK dependency) so it builds at stamp time;
see README "SDK resolution" for the wiring steps + the `NODE_AUTH_TOKEN`
requirement once you consume the published package.
