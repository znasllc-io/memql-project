// Code generated by scripts/sdk-gen. DO NOT EDIT.
// Source of truth: dsl/**/*.memql.

import { QueryClient, renderMemQLValue, type QueryCallOptions, type Result } from "@znasllc-io/memql-sdk-core";

/** Phase 2 of the direct/Guide avatar handshake. Brings the vendor cloud engine (anam/simli) up to dial into the room from startSession; call only after the browser has joined and started forwarding audio over the lk.audio_stream byte stream. Mints the avatar token (publish_on_behalf=browser_identity) and starts the session. Returns { session_id, vendor, avatar_identity, ok }. */
export interface AvatarDirectEngageVendorArgs {
  agentId: string;
  room_name: string;
  browser_identity: string;
  partitionId?: string;
}

export function buildAvatarDirectEngageVendor(args: AvatarDirectEngageVendorArgs): string {
  const parts: string[] = [];
  parts.push("agentId: " + renderMemQLValue(args.agentId));
  parts.push("room_name: " + renderMemQLValue(args.room_name));
  parts.push("browser_identity: " + renderMemQLValue(args.browser_identity));
  if (args.partitionId !== undefined) parts.push("partitionId: " + renderMemQLValue(args.partitionId));
  return "builtin avatarDirectEngageVendor(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    avatarDirectEngageVendor(args: AvatarDirectEngageVendorArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.avatarDirectEngageVendor = function (this: QueryClient, args: AvatarDirectEngageVendorArgs = {} as AvatarDirectEngageVendorArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("avatarDirectEngageVendor", buildAvatarDirectEngageVendor(args), opts);
};

/** Phase 1 of the direct/Guide avatar handshake (anam/simli). Mints a LiveKit room + an agent-kind browser join token and validates the agent has an avatar persona; does NOT start the vendor engine. The browser joins + starts forwarding audio, THEN calls avatarDirectEngageVendor (Simli waits for the audio-providing participant to be present first, memql#782). Returns { livekit_url, livekit_client_token, browser_identity, vendor, room_name }. */
export interface AvatarDirectStartSessionArgs {
  agentId: string;
  partitionId?: string;
}

export function buildAvatarDirectStartSession(args: AvatarDirectStartSessionArgs): string {
  const parts: string[] = [];
  parts.push("agentId: " + renderMemQLValue(args.agentId));
  if (args.partitionId !== undefined) parts.push("partitionId: " + renderMemQLValue(args.partitionId));
  return "builtin avatarDirectStartSession(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    avatarDirectStartSession(args: AvatarDirectStartSessionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.avatarDirectStartSession = function (this: QueryClient, args: AvatarDirectStartSessionArgs = {} as AvatarDirectStartSessionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("avatarDirectStartSession", buildAvatarDirectStartSession(args), opts);
};

/** Stop a direct/Guide avatar session. Best-effort (Anam sessions self-expire, the LiveKit room is ephemeral); always returns { ok: true }. */
export interface AvatarDirectStopSessionArgs {
  session_id?: string;
  vendor?: string;
  room_name?: string;
}

export function buildAvatarDirectStopSession(args: AvatarDirectStopSessionArgs): string {
  const parts: string[] = [];
  if (args.session_id !== undefined) parts.push("session_id: " + renderMemQLValue(args.session_id));
  if (args.vendor !== undefined) parts.push("vendor: " + renderMemQLValue(args.vendor));
  if (args.room_name !== undefined) parts.push("room_name: " + renderMemQLValue(args.room_name));
  return "builtin avatarDirectStopSession(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    avatarDirectStopSession(args: AvatarDirectStopSessionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.avatarDirectStopSession = function (this: QueryClient, args: AvatarDirectStopSessionArgs = {} as AvatarDirectStopSessionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("avatarDirectStopSession", buildAvatarDirectStopSession(args), opts);
};

/** Append a new version of a Library document with new content. Reads the current latest version, computes the next versionNumber + parentVersionId, appends an immutable v1:library:documentVersion snapshot (authorKind=user|assistant) and re-inserts the backing generatedOutput so the Library viewer reflects the edit. Optimistic concurrency via expectedVersion. ownerUserId is threaded from the document row, never the caller. Backs both the user edit (memql#1229) and the assistant editDocument tool (memql#1231). */
export interface EditDocumentArgs {
  documentId: string;
  content?: string;
  attachmentId?: string;
  note?: string;
  // Enum: user | assistant | system
  authorKind?: string;
  authorId?: string;
  expectedVersion?: number;
  producedByPlanId?: string;
  partitionId?: string;
}

export function buildEditDocument(args: EditDocumentArgs): string {
  const parts: string[] = [];
  parts.push("documentId: " + renderMemQLValue(args.documentId));
  if (args.content !== undefined) parts.push("content: " + renderMemQLValue(args.content));
  if (args.attachmentId !== undefined) parts.push("attachmentId: " + renderMemQLValue(args.attachmentId));
  if (args.note !== undefined) parts.push("note: " + renderMemQLValue(args.note));
  if (args.authorKind !== undefined) parts.push("authorKind: " + renderMemQLValue(args.authorKind));
  if (args.authorId !== undefined) parts.push("authorId: " + renderMemQLValue(args.authorId));
  if (args.expectedVersion !== undefined) parts.push("expectedVersion: " + renderMemQLValue(args.expectedVersion));
  if (args.producedByPlanId !== undefined) parts.push("producedByPlanId: " + renderMemQLValue(args.producedByPlanId));
  if (args.partitionId !== undefined) parts.push("partitionId: " + renderMemQLValue(args.partitionId));
  return "builtin editDocument(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    editDocument(args: EditDocumentArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.editDocument = function (this: QueryClient, args: EditDocumentArgs = {} as EditDocumentArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("editDocument", buildEditDocument(args), opts);
};

/** Fetch a harness plan's full execution timeline (every plan/step version transition + all observations, ordered by createdAt) reconstructed from the append-only graph event stream. Returns one synthetic node carrying the rendered timeline string, a completion flag, and the step count. Owner-scoped to the caller's own plan. The history-over-gRPC contract for the cockpit `harness trace` CLI (memql-cockpit#142). */
export interface HarnessTraceArgs {
  planId: string;
}

export function buildHarnessTrace(args: HarnessTraceArgs): string {
  const parts: string[] = [];
  parts.push("planId: " + renderMemQLValue(args.planId));
  return "builtin harnessTrace(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    harnessTrace(args: HarnessTraceArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.harnessTrace = function (this: QueryClient, args: HarnessTraceArgs = {} as HarnessTraceArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("harnessTrace", buildHarnessTrace(args), opts);
};

/** Decide whether a chat exchange's topic warrants augmenting one of an agent's knowledge domains. Returns {outcome, domainId, topic, reasoning, confidence}. Drives the chat 'Analyze for training' button preflight. */
export interface KnowledgeAugmentDomainAnalyzeArgs {
  userQuestion: string;
  agentResponse: string;
  domains: Record<string, unknown>[];
  retrieved?: Record<string, unknown>[];
}

export function buildKnowledgeAugmentDomainAnalyze(args: KnowledgeAugmentDomainAnalyzeArgs): string {
  const parts: string[] = [];
  parts.push("userQuestion: " + renderMemQLValue(args.userQuestion));
  parts.push("agentResponse: " + renderMemQLValue(args.agentResponse));
  parts.push("domains: " + renderMemQLValue(args.domains));
  if (args.retrieved !== undefined) parts.push("retrieved: " + renderMemQLValue(args.retrieved));
  return "builtin knowledgeAugmentDomainAnalyze(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    knowledgeAugmentDomainAnalyze(args: KnowledgeAugmentDomainAnalyzeArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.knowledgeAugmentDomainAnalyze = function (this: QueryClient, args: KnowledgeAugmentDomainAnalyzeArgs = {} as KnowledgeAugmentDomainAnalyzeArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("knowledgeAugmentDomainAnalyze", buildKnowledgeAugmentDomainAnalyze(args), opts);
};

/** Generate + embed + persist topic-focused chunks for a knowledge domain. Inserts a Plan row for audit. Returns {planId, chunksAdded, domainId, domainName, topic}. */
export interface KnowledgeAugmentDomainGenerateArgs {
  domainId: string;
  topic: string;
  sourceUtteranceId?: string;
  sourceAgentId?: string;
  partitionId: string;
  requestedBy: string;
}

export function buildKnowledgeAugmentDomainGenerate(args: KnowledgeAugmentDomainGenerateArgs): string {
  const parts: string[] = [];
  parts.push("domainId: " + renderMemQLValue(args.domainId));
  parts.push("topic: " + renderMemQLValue(args.topic));
  if (args.sourceUtteranceId !== undefined) parts.push("sourceUtteranceId: " + renderMemQLValue(args.sourceUtteranceId));
  if (args.sourceAgentId !== undefined) parts.push("sourceAgentId: " + renderMemQLValue(args.sourceAgentId));
  parts.push("partitionId: " + renderMemQLValue(args.partitionId));
  parts.push("requestedBy: " + renderMemQLValue(args.requestedBy));
  return "builtin knowledgeAugmentDomainGenerate(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    knowledgeAugmentDomainGenerate(args: KnowledgeAugmentDomainGenerateArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.knowledgeAugmentDomainGenerate = function (this: QueryClient, args: KnowledgeAugmentDomainGenerateArgs = {} as KnowledgeAugmentDomainGenerateArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("knowledgeAugmentDomainGenerate", buildKnowledgeAugmentDomainGenerate(args), opts);
};

/** Recall top-k memories of a concept (default v1:harness:observation) by a SINGLE hybrid recency x relevance score: pgvector cosine similarity + exponential time-decay over createdAt, scored and ordered server-side in one SQL statement against the MemoryNodes hypertable (no app-side merge). Owner-scoped (partition isolation); window prunes hypertable chunks; halfLife + wSem/wRec are tunable. Numeric tuning args ride additionalProperties. */
export interface RecallArgs {
  text: string;
  concept?: string;
  k?: number;
  provider?: string;
}

export function buildRecall(args: RecallArgs): string {
  const parts: string[] = [];
  parts.push("text: " + renderMemQLValue(args.text));
  if (args.concept !== undefined) parts.push("concept: " + renderMemQLValue(args.concept));
  if (args.k !== undefined) parts.push("k: " + renderMemQLValue(args.k));
  if (args.provider !== undefined) parts.push("provider: " + renderMemQLValue(args.provider));
  return "builtin recall(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    recall(args: RecallArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.recall = function (this: QueryClient, args: RecallArgs = {} as RecallArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("recall", buildRecall(args), opts);
};

/** Restore a Library document to an earlier version by APPENDING a new latest version equal to the chosen one (memql#1230). Forward-only and non-destructive: history is never deleted; the restore lands as a new version (authorKind=system) with note 'restored from vN'. ownerUserId is threaded from the document row. */
export interface RestoreDocumentVersionArgs {
  documentId: string;
  versionId: string;
  authorId?: string;
}

export function buildRestoreDocumentVersion(args: RestoreDocumentVersionArgs): string {
  const parts: string[] = [];
  parts.push("documentId: " + renderMemQLValue(args.documentId));
  parts.push("versionId: " + renderMemQLValue(args.versionId));
  if (args.authorId !== undefined) parts.push("authorId: " + renderMemQLValue(args.authorId));
  return "builtin restoreDocumentVersion(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    restoreDocumentVersion(args: RestoreDocumentVersionArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.restoreDocumentVersion = function (this: QueryClient, args: RestoreDocumentVersionArgs = {} as RestoreDocumentVersionArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("restoreDocumentVersion", buildRestoreDocumentVersion(args), opts);
};

/** searchActions wraps the builtin named "searchActions". */
export interface SearchActionsArgs {
  text: string;
  k?: number;
  provider?: string;
}

export function buildSearchActions(args: SearchActionsArgs): string {
  const parts: string[] = [];
  parts.push("text: " + renderMemQLValue(args.text));
  if (args.k !== undefined) parts.push("k: " + renderMemQLValue(args.k));
  if (args.provider !== undefined) parts.push("provider: " + renderMemQLValue(args.provider));
  return "builtin searchActions(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    searchActions(args: SearchActionsArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.searchActions = function (this: QueryClient, args: SearchActionsArgs = {} as SearchActionsArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("searchActions", buildSearchActions(args), opts);
};

/** Train an agent: replace capabilities.domains + .tools, eager-embed chunks, refresh identity vector, distill system prompt. Bracketed by a Plan + 3 Tasks for canvas + Tasks-page visibility. */
export interface TrainAgentArgs {
  agentId: string;
  domains?: unknown[];
  tools?: unknown[];
  provider?: string;
  partitionId?: string;
  requestedBy?: string;
}

export function buildTrainAgent(args: TrainAgentArgs): string {
  const parts: string[] = [];
  parts.push("agentId: " + renderMemQLValue(args.agentId));
  if (args.domains !== undefined) parts.push("domains: " + renderMemQLValue(args.domains));
  if (args.tools !== undefined) parts.push("tools: " + renderMemQLValue(args.tools));
  if (args.provider !== undefined) parts.push("provider: " + renderMemQLValue(args.provider));
  if (args.partitionId !== undefined) parts.push("partitionId: " + renderMemQLValue(args.partitionId));
  if (args.requestedBy !== undefined) parts.push("requestedBy: " + renderMemQLValue(args.requestedBy));
  return "builtin trainAgent(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    trainAgent(args: TrainAgentArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.trainAgent = function (this: QueryClient, args: TrainAgentArgs = {} as TrainAgentArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("trainAgent", buildTrainAgent(args), opts);
};

/** Re-run a single failed training step (Step B identity vector OR Step C distilled prompt) for an agent. Used by the background retry poll loop and the frontend Retry button. */
export interface TrainAgentRetryStepArgs {
  agentId: string;
  step: string;
  provider?: string;
  retryPlanId?: string;
  originalPlanId?: string;
}

export function buildTrainAgentRetryStep(args: TrainAgentRetryStepArgs): string {
  const parts: string[] = [];
  parts.push("agentId: " + renderMemQLValue(args.agentId));
  parts.push("step: " + renderMemQLValue(args.step));
  if (args.provider !== undefined) parts.push("provider: " + renderMemQLValue(args.provider));
  if (args.retryPlanId !== undefined) parts.push("retryPlanId: " + renderMemQLValue(args.retryPlanId));
  if (args.originalPlanId !== undefined) parts.push("originalPlanId: " + renderMemQLValue(args.originalPlanId));
  return "builtin trainAgentRetryStep(" + parts.join(", ") + ")";
}

declare module "@znasllc-io/memql-sdk-core" {
  interface QueryClient {
    trainAgentRetryStep(args: TrainAgentRetryStepArgs, opts?: QueryCallOptions): Promise<Result>;
  }
}

QueryClient.prototype.trainAgentRetryStep = function (this: QueryClient, args: TrainAgentRetryStepArgs = {} as TrainAgentRetryStepArgs, opts?: QueryCallOptions): Promise<Result> {
  return this.executeNamed("trainAgentRetryStep", buildTrainAgentRetryStep(args), opts);
};

