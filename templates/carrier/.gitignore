# Test artifacts
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace file (per-developer, not shared)
go.work
go.work.sum

# Editor / debug
__debug*
.idea/
.vscode/

# macOS
.DS_Store

# Local env override (see memql/component/genesis/localenv.go).
# Each developer's values are theirs; production binaries don't
# carry a .env at all.
.env
.env.local
